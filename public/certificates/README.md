# Certificate PDFs

Place certificate PDF files in this directory using the slug-based filenames referenced by `CERTIFICATES` in `utils/portfolioContent.ts`.

The portfolio intentionally renders preview and download actions before the PDF assets are available. A reference such as `/certificates/advanced-git.pdf` resolves to `public/certificates/advanced-git.pdf`.
